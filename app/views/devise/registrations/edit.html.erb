<div class="flex justify-center mt-20 ">
  <div class="container pb-8 border-b-4 border-green-500 px-20 flex flex-wrap">
    <div class="flex  ml-20">
      <%= image_tag "coming_soon.png", class: "sm:w-[100px] md:w-[200px]"%>
      <div>
        <div class="flex items-center text-gray-600 ml-20 mt-5 font-bold">
          <div class="text-4xl">
            <%= current_user.name%>
          </div>
          <div class="text-3xl ml-4 flex items-end">
            <div><%= "Lv.#{current_user.profile.level}"%></div>
            <div class="text-xs ml-3 mb-1">次のレベルまで<span class="text-green-500">■</span>×<%= "#{10 - current_user.profile.experience_points}"%></div>
          </div>
        </div>
        <div class="ml-20 mt-10 font-bold">
          <div class="text-gray-600 text-2xl">
            <a>本日の戦闘力</a>
            <a class="ml-2 text-3xl"><%= current_user.profile.week_contributions%>万</a>
          </div>
          <div class="mt-5">
            <a>多分これからステータスとかが色々入るぞ？</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<div class="mx-auto flex min-h-screen w-full items-center justify-center text-gray-600">
    <div class="flex w-[30rem] flex-col space-y-5">
    <h2 class="text-center text-2xl font-medium">編集</h2>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

        <div class="w-full mt-4 transform border-b-2 bg-transparent text-lg duration-300 focus-within:border-green-500">
          <%= f.text_field :name, autofocus: true, autocomplete: "name" ,placeholder: "名前", class: "w-full border-none bg-transparent outline-none placeholder:italic focus:outline-none"%>
        </div>

        <div class="w-full mt-4 transform border-b-2 bg-transparent text-lg duration-300 focus-within:border-green-500">
          <%= f.text_field :github_name, autofocus: true, autocomplete: "github_name", placeholder: "GitHub ID", class: "w-full border-none bg-transparent outline-none placeholder:italic focus:outline-none" %>
        </div>


        <div class="w-full mt-4 transform border-b-2 bg-transparent text-lg duration-300 focus-within:border-green-500">
          <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "メールアドレス", class: "w-full border-none bg-transparent outline-none placeholder:italic focus:outline-none" %>
        </div>

        <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
          <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
        <% end %>

        <div>
          <%= f.submit "更新する", class: "text-white w-full mt-12 transform rounded-sm bg-green-600 py-2 font-bold duration-300 hover:bg-green-400" %>
        </div>
      <% end %>
      <%= button_to "アカウントを削除する", registration_path(resource_name), data: { confirm: "削除してよろしいですか？", turbo_confirm: "削除してよろしいですか？" }, method: :delete , class: "text-white w-full transform rounded-sm bg-red-400 py-2 font-bold duration-300 hover:bg-red-600" %>
    <%= link_to "戻る", :back, class: "text-center font-medium text-green-500 underline-offset-4 hover:underline" %>
  </div>
</div>
